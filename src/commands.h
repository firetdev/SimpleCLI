#pragma once

#include <iostream>
#include <map>
#include <string>
#include <functional>

std::vector<std::string> commandNames;
std::vector<std::function<std::string(std::string)>> commandFuncs
{
	
};
